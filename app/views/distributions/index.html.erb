<h1>Distributions</h1>
<details>
  <summary>What is a Distribution?</summary>
  <p>Every week on a <%= DistributionDateService::DAY_OF_THE_WEEK %>, the pool will be dividend equally between each active member and distributed in the form of a dividend.</p>
</details>
<% if @distributions.any? %>
  <% @distributions.each do |distribution| %>
    <article>
      <p><b>Date</b>: <%= created_at_formatted distribution %></p> 
      <p><b>Dividend</b>: <%= distribution.dividend_amount_formatted %></p>
      <p><b>Members</b>: <%= distribution.dividends.length %></p>
    </article>
  <% end %>
<% else %>
  <article>
    <p>Distributions will appear here.</p>
  </article>
<% end %>
