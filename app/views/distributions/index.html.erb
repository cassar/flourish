<h1>Distributions</h1>
<% if @distributions.any? %>
  <% @distributions.each do |distribution| %>
    <article>
      <p><b>Date</b>: <%= created_at_formatted distribution %></p> 
      <p><b>Dividend</b>: <%= distribution.dividend_amount_formatted %></p>
      <p><b>Members</b>: <%= distribution.dividends.length %></p>
    </article>
  <% end %>
<% else %>
  <article>
    <p>Distributions will appear here.</p>
  </article>
<% end %>
<details>
  <summary>About Distributions</summary>
  <p>Distributions will occur on a <%= DistributionDateService::DAY_OF_THE_WEEK %>.</p> 
  <p>Members will then be given the option to have their dividend paid out, or to recontribute the money back into the pool.</p>
</details>
