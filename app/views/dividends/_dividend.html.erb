<article>
  <p><b>Date</b>: <%= created_at_formatted dividend %></p> 
  <p><b>Amount</b>: <%= dividend.distribution.dividend_amount_formatted %></p>
  <% if dividend.issued? %>
    <%= button_to "Pay Out", pay_out_dividend_path(dividend), method: :patch, disabled: @member.pay_outs_disabled? %>
    <%= button_to "Recontribute", recontribute_dividend_path(dividend), method: :patch %>
  <% else %>
    <p><b>Status</b>: <%= dividend.status.titleize %></p>
  <% end %>
  <% if dividend.pay_out_complete? %>
    <details>
      <summary>Transaction Identifier</summary>
      <p><%= dividend.transaction_identifier %></p>
    </details>
  <% end %>
</article>
